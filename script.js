let firstString = "I am";
let secondString = " in love with JavaScript";
let newString = firstString + secondString;
console.log (newString);

/* string concatenation is joining character strings end to end. it joins 2 or more strings, use the '+' operator, the 'concat' method or 'template literals' to jion 2or more strings. */